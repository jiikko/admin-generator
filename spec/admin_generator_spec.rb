require 'spec_helper'

describe AdminGenerator do
  it 'has a version number' do
    expect(AdminGenerator::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
